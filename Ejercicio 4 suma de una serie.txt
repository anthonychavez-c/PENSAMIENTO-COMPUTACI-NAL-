#include <iostream>
#include <cmath>
using namespace std;
int main (){
    float a, suma, i;
    cin>>a;
    suma=0;
    i=1;
    while(i<=a)
    {
        suma=suma+pow(i,a-i+1);
        i++;
    }
    cout<<suma;
    return 0;
}